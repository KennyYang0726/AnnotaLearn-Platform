## `DATABASE_URL` 是做什麼的？

`DATABASE_URL` 是後端連線 PostgreSQL 的「資料庫地址」。Prisma 會使用它來知道：

- 要連到哪一台資料庫主機
- 使用哪個帳號與密碼
- PostgreSQL 使用哪個 port
- 要操作哪一個 database

本機範例：

```text
postgresql://annotalearn:annotalearn@localhost:5432/annotalearn?schema=public
             │          │          │         │        │
             使用者      密碼        主機      Port     Database
```

因此 `.env.example` 與 `docker-compose.yml` 的帳號、密碼、Database 名稱必須一致。

部署到 Vercel 時，不會使用本機的 `localhost`。請改成 Neon、Prisma Postgres 或其他 PostgreSQL 服務提供的正式 `DATABASE_URL`，並把它設定在 Vercel Project Settings → Environment Variables，而不是寫進 GitHub。

---

## 為什麼 `prisma/migrations` 要留在 GitHub？

`prisma/migrations` **不是現存資料庫，也不包含目前的學生、課程或筆記資料**。

它只是一組資料庫結構的版本紀錄，例如「建立 User 表」、「新增 schoolEmail 欄位」。Clone 專案的人執行：

```bash
npm run db:deploy
```

Prisma 才會在對方自己的 PostgreSQL 中建立相同的表格結構。

因此 migrations 應該保留並提交到 Git；真正的 PostgreSQL 資料不會放在 repository 裡。本機 Docker 的資料存在 Docker named volume `annotalearn_pgdata`，不在專案資料夾內。

如果要把自己的本機資料庫完全清空：

```bash
docker compose down -v
docker compose up -d
npm run db:deploy
npm run db:seed
```

`docker compose down -v` 會刪除該 Docker volume 中的資料，請只在確定要重建本機 DB 時使用。
